// server.cfg

// Allgemeine Einstellungen
hostname = "${ARMA_HOSTNAME}";
password = "${ARMA_PASSWORD}";
passwordAdmin = "${ARMA_PASSWORD_ADMIN}";
maxPlayers = ${ARMA_MAX_PLAYERS};
persistent = ${ARMA_PERSISTENT};
battleye = ${ARMA_BATTLEYE};

// Missionsrotation
class Missions
{
  class Mission1
  {
    template = "${ARMA_MISSION}";
    difficulty = "${ARMA_DIFFICULTY}";
  };
};

// Weitere Einstellungen können hier hinzugefügt werden
